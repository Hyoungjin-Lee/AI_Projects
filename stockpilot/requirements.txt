# stockpilot — Python 의존성
#
# 설치:
#   venv/bin/python3 -m pip install -r requirements.txt
#
# 환경: Python 3.14 (venv/)
# 마지막 갱신: 2026-05-06 (v2.8.7 — holidays 추가)

# === 핵심 의존성 ===
requests>=2.33                  # KIS Open API HTTP
python-dotenv>=1.2              # .env 로드 (Keychain 보조)

# === 시장 데이터 ===
yfinance>=1.3                   # 글로벌 지수 (S&P500/나스닥/KOSPI ^KS11)

# === 분석/처리 ===
pandas>=3.0
numpy>=2.4

# === 캘린더 ===
holidays>=0.95                  # 한국 공휴일 (market_calendar.py)

# === KIS API 문서 점검 (개발용) ===
openpyxl>=3.1                   # docs/api/*.xlsx 검색

# === 테스트 ===
pytest>=9.0
